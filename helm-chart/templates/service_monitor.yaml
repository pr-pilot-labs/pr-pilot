apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    release: prometheus-grafana
  name: django-app
spec:
  endpoints:
  - interval: 30s
    path: /metrics
  selector:
    matchLabels:
      app: pr-pilot
